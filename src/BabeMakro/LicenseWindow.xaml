<Window x:Class="BabeMakro.LicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BabeMakro - License Activation"
        Height="450"
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#44475A"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="CharacterCasing" Value="Upper"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#6272A4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#6272A4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7082B6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5A6A9C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#44475A"/>
                                <Setter Property="Foreground" Value="#6C6F85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#44475A"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#565A6E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A3D4E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E2E" CornerRadius="10" BorderBrush="#44475A" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#282A36" CornerRadius="10,10,0,0" MouseLeftButtonDown="Border_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="BabeMakro License Activation"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Left"
                             Margin="15,0,0,0"
                             Foreground="White"
                             FontSize="14"
                             FontWeight="SemiBold"/>

                    <Button Grid.Column="1"
                          Content="✕"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"
                          Background="Transparent"
                          Foreground="White"
                          BorderThickness="0"
                          FontSize="16"
                          Width="30"
                          Height="30"
                          Cursor="Hand"
                          Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="40,0">
                <Border Background="#282A36"
                       CornerRadius="5"
                       Padding="15"
                       Margin="0,0,0,30">
                    <StackPanel>
                        <TextBlock FontSize="16" Foreground="#FFB86C" FontWeight="SemiBold" Margin="0,0,0,10">
                            <Run Text="⚠"/> License Required
                        </TextBlock>
                        <TextBlock Text="Your license has expired or is not activated."
                                 Foreground="#F8F8F2"
                                 FontSize="13"
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,5"/>
                        <TextBlock Text="Please enter a valid license key to continue using BabeMakro."
                                 Foreground="#6C6F85"
                                 FontSize="12"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="License Key"
                         Foreground="#6C6F85"
                         FontSize="12"
                         Margin="0,0,0,8"/>
                <TextBox x:Name="LicenseKeyTextBox"
                        Style="{StaticResource ModernTextBox}"
                        Margin="0,0,0,20"
                        FontFamily="Consolas"
                        KeyDown="LicenseKeyTextBox_KeyDown"
                        TextChanged="LicenseKeyTextBox_TextChanged"/>

                <TextBlock x:Name="LicenseKeyFormatText"
                         Text="Format: XXXX-XXXX-XXXX-XXXX"
                         FontSize="11"
                         Foreground="#6C6F85"
                         Margin="0,-15,0,20"/>

                <Button x:Name="ActivateButton"
                       Content="ACTIVATE LICENSE"
                       Style="{StaticResource ModernButton}"
                       Click="ActivateButton_Click"
                       Margin="0,0,0,15"/>

                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                           x:Name="LogoutButton"
                           Content="LOGOUT"
                           Style="{StaticResource SecondaryButton}"
                           Click="LogoutButton_Click"/>

                    <Button Grid.Column="2"
                           x:Name="RefreshButton"
                           Content="REFRESH"
                           Style="{StaticResource SecondaryButton}"
                           Click="RefreshButton_Click"/>
                </Grid>

                <ProgressBar x:Name="LoadingProgressBar"
                           IsIndeterminate="True"
                           Height="3"
                           Margin="0,20,0,0"
                           Visibility="Collapsed"
                           Foreground="#6272A4"
                           Background="Transparent"
                           BorderThickness="0"/>

                <TextBlock x:Name="StatusText"
                         Text=""
                         FontSize="12"
                         HorizontalAlignment="Center"
                         Margin="0,10,0,0"
                         TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>